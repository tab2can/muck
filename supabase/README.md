# Dashboard'da SQL Editor → supabase/migrations/001_init.sql çalıştır.
# Authentication → Providers → Email: Enable + Confirm email
# Authentication → URL Configuration:
#   Site URL: https://muck.tr  (local: http://localhost:3000)
#   Redirect URLs: https://muck.tr/login , http://localhost:3000/login , https://muck.tr/reset-password
#
# API Keys: Publishable + Secret kullan (anon / service_role legacy değil).
